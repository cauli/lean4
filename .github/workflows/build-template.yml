# Instantiated by ci.yml and browser.yml.
name: build-template
on:
  workflow_call:
    inputs:
      config:
        type: string
        required: true
      nightly:
        type: string
        required: true
      LEAN_VERSION_MAJOR:
        type: string
        required: true
      LEAN_VERSION_MINOR:
        type: string
        required: true
      LEAN_VERSION_PATCH:
        type: string
        required: true
      LEAN_SPECIAL_VERSION_DESC:
        type: string
        required: true
      RELEASE_TAG:
        type: string
        required: true

jobs:
  build:
    if: github.event_name != 'schedule' || github.repository == 'leanprover/lean4'
    strategy:
      matrix:
        include: ${{fromJson(inputs.config)}}
      # complete all jobs
      fail-fast: false
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: ${{ matrix.shell || 'nix develop -c bash -euxo pipefail {0}' }}
    name: ${{ matrix.name }}
    env:
      # must be inside workspace
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      CCACHE_COMPRESS: true
      # current cache limit
      CCACHE_MAXSIZE: ${{ matrix.wasm && '1600M' || '400M' }}
      # squelch error message about missing nixpkgs channel
      NIX_BUILD_SHELL: bash
      LSAN_OPTIONS: max_leaks=10
      # somehow MinGW clang64 (or cmake?) defaults to `g++` even though it doesn't exist
      CXX: c++
      MACOSX_DEPLOYMENT_TARGET: 11.0
    steps:
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@ef8a148080ab6020fd15196c2084a2eea5ff2d25 # v22
        if: runner.os == 'Linux' && !matrix.cmultilib
        with:
          summarize: false
      - name: Install MSYS2
        uses: msys2/setup-msys2@66cd2cce69caa17b53920067426061ca1de3a884 # v2.32.0
        with:
          msystem: clang64
          # `:` means do not prefix with msystem
          pacboy: "make: python: cmake clang ccache gmp libuv openssl: git: zip: unzip: diffutils: binutils: tree: zstd tar:"
        if: runner.os == 'Windows'
      - name: Install Brew Packages
        run: |
          brew install ccache tree zstd coreutils gmp libuv openssl
        if: runner.os == 'macOS'
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # the default is to use a virtual merge commit between the PR and master: just use the PR
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Open Nix shell once
        run: true
        if: runner.os == 'Linux'
      # Do check out some CI-relevant files from virtual merge commit to accommodate CI changes on
      # master (as the workflow files themselves are always taken from the merge)
      # (needs to be after "Install *" to use the right shell)
      - name: CI Merge Checkout
        run: |
          git fetch --depth=${{ matrix.name == 'Linux Lake (Cached)' && '10' || '1' }} origin ${{ github.sha }}
          git checkout FETCH_HEAD flake.nix flake.lock script/prepare-* tests/elab/importStructure.lean
        if: github.event_name == 'pull_request'
      # (needs to be after "Checkout" so files don't get overridden)
      - name: Setup emsdk
        uses: emscripten-core/setup-emsdk@4528d102f7230f0e7b276855c01ea1159be0e984 # v16
        with:
          version: 4.0.22 # emsdk 6 pool workers crash at boot (growMemViews before the load handshake); 4.0.22 pthread bootstrap is proven
          actions-cache-folder: emsdk
        if: matrix.wasm
      - name: Install 32bit c libs
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y gcc-multilib g++-multilib ccache libuv1-dev:i386 libssl-dev:i386 pkgconf:i386
        if: matrix.cmultilib
      - name: Check Emscripten shell glue
        if: matrix.wasm
        run: python3 tests/wasm/check-shell-glue.py
      - name: Restore Cache
        id: restore-cache
        uses: actions/cache/restore@v6
        with:
          # NOTE: must be in sync with `save` below and with `restore-cache` in `update-stage0.yml`
          path: |
            .ccache
            ${{ matrix.lake-actions-cache && 'build/stage1/**/*.trace
            build/stage1/**/*.olean*
            build/stage1/**/*.ilean
            build/stage1/**/*.ir
            build/stage1/**/*.c
            build/stage1/**/*.c.o*' || '' }}
          key: ${{ matrix.name }}-build-v5-${{ runner.os }}-${{ runner.arch }}-emsdk4.0.22-${{ hashFiles('src/stdlib_flags.h', '.github/browser-build-matrix.json') }}-${{ github.sha }}
          # fall back to (latest) previous cache
          restore-keys: |
            ${{ matrix.name }}-build-v5-${{ runner.os }}-${{ runner.arch }}-emsdk4.0.22-${{ hashFiles('src/stdlib_flags.h', '.github/browser-build-matrix.json') }}-
      # open nix-shell once for initial setup
      - name: Setup
        run: |
          ccache --zero-stats
        if: runner.os == 'Linux'
      - name: Set up env
        run: |
          echo "NPROC=$(nproc 2>/dev/null || sysctl -n hw.logicalcpu 2>/dev/null || echo 4)" >> $GITHUB_ENV
          if ! diff src/stdlib_flags.h stage0/src/stdlib_flags.h; then
            echo "src/stdlib_flags.h and stage0/src/stdlib_flags.h differ, will test and pack stage 2"
            echo "TARGET_STAGE=stage2" >> $GITHUB_ENV
          else
            echo "TARGET_STAGE=stage1" >> $GITHUB_ENV
          fi
      - name: Configure Build
        run: |
          ulimit -c unlimited  # coredumps

          # For 32-bit cross-compilation (e.g., Web Assembly), configure pkg-config
          # to find 32-bit libraries instead of the default 64-bit ones.
          # Persisted via GITHUB_ENV because stage0's CMake configure runs in the
          # "Build Stage 0 & Configure Stage 1" step, not here.
          #
          # We verify libuv is findable since it's a required dependency and cmake's
          # FindLibUV.cmake uses pkg-config to locate it.
          if [[ -n '${{ matrix.cmultilib }}' ]]; then
            export PKG_CONFIG_LIBDIR=/usr/lib/i386-linux-gnu/pkgconfig:/usr/share/pkgconfig
            echo "PKG_CONFIG_LIBDIR=$PKG_CONFIG_LIBDIR" >> "$GITHUB_ENV"
            echo "PKG_CONFIG_LIBDIR=$PKG_CONFIG_LIBDIR"
            if ! pkg-config --exists libuv; then
              echo "::error::[32-bit cross-compilation] pkg-config could not find required dependency 'libuv'."
              exit 1
            fi
          fi
          
          [ -d build ] || mkdir build
          cd build
          # arguments passed to `cmake`
          # opt into `LEAN_HEADER_SNAPSHOTS` as suspected issue with incremental builds does not
          # affect CI
          OPTIONS=(-DWFAIL=ON)
          if [[ -n '${{ matrix.browser }}' ]]; then
            # Native i386 snapshots cannot disambiguate overlapping saved dep ranges.
            OPTIONS+=(-DLEAN_HEADER_SNAPSHOTS=OFF)
          else
            OPTIONS+=(-DLEAN_HEADER_SNAPSHOTS=ON)
          fi
          if [[ -n '${{ matrix.release }}' ]]; then
            # this also enables githash embedding into stage 1 library, which prohibits reusing
            # `.olean`s across commits, so we don't do it in the fast non-release CI
            OPTIONS+=(-DCHECK_OLEAN_VERSION=ON)
          fi
          if [[ -n '${{ matrix.cross_target }}' ]]; then
            # used by `prepare-llvm`
            export EXTRA_FLAGS=--target=${{ matrix.cross_target }}
            OPTIONS+=(-DLEAN_PLATFORM_TARGET=${{ matrix.cross_target }})
          fi
          if [[ -n '${{ matrix.prepare-llvm }}' ]]; then
            wget -q ${{ matrix.llvm-url }}
            PREPARE="$(${{ matrix.prepare-llvm }})"
            cp -r stage1 stage2
            cp -r stage1 stage3
            eval "OPTIONS+=($PREPARE)"
          fi
          if [[ -n '${{ matrix.release }}' && -n '${{ inputs.nightly }}' ]]; then
            OPTIONS+=(-DLEAN_SPECIAL_VERSION_DESC=${{ inputs.nightly }})
          fi
          if [[ -n '${{ matrix.release }}' && -n '${{ inputs.RELEASE_TAG }}' ]]; then
            OPTIONS+=(-DLEAN_VERSION_MAJOR=${{ inputs.LEAN_VERSION_MAJOR }})
            OPTIONS+=(-DLEAN_VERSION_MINOR=${{ inputs.LEAN_VERSION_MINOR }})
            OPTIONS+=(-DLEAN_VERSION_PATCH=${{ inputs.LEAN_VERSION_PATCH }})
            OPTIONS+=(-DLEAN_VERSION_IS_RELEASE=1)
            OPTIONS+=(-DLEAN_SPECIAL_VERSION_DESC=${{ inputs.LEAN_SPECIAL_VERSION_DESC }})
          fi
          # contortion to support empty OPTIONS with old macOS bash
          cmake .. --preset ${{ matrix.CMAKE_PRESET || 'release' }} -B . ${{ matrix.CMAKE_OPTIONS }} ${OPTIONS[@]+"${OPTIONS[@]}"} -DLEAN_INSTALL_PREFIX=$PWD/..
      - name: Build Stage 0 & Configure Stage 1
        run: |
          ulimit -c unlimited  # coredumps
          # Increase Node.js heap size for Emscripten's acorn-optimizer when building WASM.
          # The lean.js file is very large (~90MB) and the optimizer runs out of memory
          # with the default heap size. See: https://github.com/emscripten-core/emscripten/issues/24558
          # Exported inline because GitHub Actions refuses to persist NODE_OPTIONS via GITHUB_ENV.
          if [[ -n '${{ matrix.wasm }}' ]]; then
            export NODE_OPTIONS="--max-old-space-size=8192"
            # Route emcc's clang invocations through ccache: the ~2,500
            # generated-C compiles are ~13 min of the job and are content-stable
            # across most commits. 400M (the global cap) evicts most of that
            # object set, so raise it for this job only.
            export EM_COMPILER_WRAPPER=ccache
            export CCACHE_MAXSIZE=1600M
          fi
          time make -C build stage1-configure -j$NPROC
      - name: Download Lake Cache
        if: matrix.name == 'Linux Lake (Cached)'
        run: |
          cd src
          ../build/stage0/bin/lake cache get --repo=${{ github.repository }}
        timeout-minutes: 20 # prevent excessive hanging from network issues
        continue-on-error: true
      - name: Build Target Stage
        timeout-minutes: ${{ matrix.build-timeout || 360 }}
        run: |
          ulimit -c unlimited  # coredumps
          # See "Build Stage 0 & Configure Stage 1" for why this is exported inline.
          if [[ -n '${{ matrix.wasm }}' ]]; then
            export NODE_OPTIONS="--max-old-space-size=8192"
            export EM_COMPILER_WRAPPER=ccache
            export CCACHE_MAXSIZE=1600M
          fi
          if [[ -n '${{ matrix.browser }}' && -z '${{ matrix.wasm }}' ]]; then
            # Lake accepts one compiler executable, not CMake's launcher prefix.
            test -x /usr/lib/ccache/clang
            time make -C build $TARGET_STAGE -j$NPROC LEAN_CC=/usr/lib/ccache/clang
          else
            time make -C build $TARGET_STAGE -j$NPROC
          fi
      - name: Pack wasm link kit
        id: link-kit
        if: matrix.wasm && !cancelled()
        run: |
          # Everything the final `bin/lean.js` link consumes, so link-flag
          # experiments can relink locally (emsdk runs natively on any host)
          # instead of paying a full CI cycle. Consumed by
          # docker-wasm/relink-local.sh.
          mkdir -p linkkit
          find "build/$TARGET_STAGE" -name '*.a' -exec cp {} linkkit/ \;
          find "build/$TARGET_STAGE/libuv" -name 'libuv*.a' -exec cp {} linkkit/ \;
          cp "build/$TARGET_STAGE/stdlib.make" linkkit/stdlib.make.txt
          cp "build/$TARGET_STAGE/leanc.sh" linkkit/leanc.sh.txt
          cp "build/$TARGET_STAGE/emscripten-exports.txt" "build/$TARGET_STAGE/emscripten-exports-slim.txt" linkkit/
          emcc --version > linkkit/emscripten-version.txt
          git rev-parse HEAD > linkkit/githash.txt
          ls -la linkkit/
          tar cf - linkkit | zstd -T0 --no-progress -o linkkit.tar.zst
      - uses: actions/upload-artifact@v7
        if: matrix.wasm && !cancelled() && steps.link-kit.outcome == 'success'
        with:
          name: linkkit-${{ matrix.name }}
          path: linkkit.tar.zst
          compression-level: 0
          retention-days: 30
      - name: Dump wasm link configuration on failure
        if: failure() && matrix.wasm
        run: |
          echo "=== build/stage0/leanc.sh ==="
          cat build/stage0/leanc.sh || true
          echo "=== bin/lean link rule from build/stage0/stdlib.make ==="
          grep -B2 -A4 'bin/lean\$' build/stage0/stdlib.make || true
          echo "=== which linkers exist ==="
          command -v lld ld.lld ld || true
      # Should be done as early as possible and in particular *before* "Check rebootstrap" which
      # changes the state of stage1/
      - name: Save Cache
        # Caching on cancellation created some mysterious issues perhaps related to improper build
        # shutdown
        if: steps.restore-cache.outputs.cache-hit != 'true' && !cancelled()
        uses: actions/cache/save@v6
        with:
          # NOTE: must be in sync with `restore` above
          path: |
            .ccache
            ${{ matrix.lake-actions-cache && 'build/stage1/**/*.trace
            build/stage1/**/*.olean*
            build/stage1/**/*.ilean
            build/stage1/**/*.ir
            build/stage1/**/*.c
            build/stage1/**/*.c.o*' || '' }}
          key: ${{ steps.restore-cache.outputs.cache-primary-key }}
      - id: upload-lake-cache
        name: Upload Lake Cache
        # Caching on cancellation created some mysterious issues perhaps related to improper build
        # shutdown. Also, since this needs access to secrets, it cannot be run on forks.
        if: matrix.name == 'Linux release' && !cancelled() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
        run: |
          curl --version
          cd src
          time ../build/stage0/bin/lake build -o ../build/lake-mappings.jsonl
          time ../build/stage0/bin/lake cache put ../build/lake-mappings.jsonl --repo=${{ github.repository }}
        env:
          LAKE_CACHE_KEY: ${{ secrets.LAKE_CACHE_KEY }}
          LAKE_CACHE_ARTIFACT_ENDPOINT: ${{ vars.LAKE_CACHE_ENDPOINT }}/a1
          LAKE_CACHE_REVISION_ENDPOINT: ${{ vars.LAKE_CACHE_ENDPOINT }}/r1
        timeout-minutes: 20 # prevent excessive hanging from network issues
        continue-on-error: true
      - name: Verify Lake Cache
        if: steps.upload-lake-cache.outcome == 'success'
        run: |
          cp -r build/stage1 build/stage1.bak
          rm -rf build/stage1/lib
          cd src
          ../build/stage0/bin/lake cache clean
          ../build/stage0/bin/lake cache get --repo=${{ github.repository }}
          ../build/stage0/bin/lake build --no-build
        continue-on-error: true
      - name: Restore Build
        if: steps.upload-lake-cache.outcome == 'success'
        run: |
          build/stage0/bin/lake cache clean
          rm -rf build/stage1
          mv build/stage1.bak build/stage1
      - name: Install
        run: |
          # Increase Node.js heap size for Emscripten's acorn-optimizer when building WASM.
          if [[ -n '${{ matrix.wasm }}' ]]; then
            export NODE_OPTIONS="--max-old-space-size=8192"
          fi
          make -C build/$TARGET_STAGE install
      - name: Link slim (iOS) wasm variant
        if: matrix.wasm
        run: |
          # Second binary from the same build tree: everything except the
          # ~88k boxed wrappers. Boxed exports keep external-package resident
          # imports browser-safe but cost ~450 bytes of glue each — the slim
          # variant trades those imports (the app falls back to one-shot runs)
          # for a binary small enough for iPhone WebKit's tab memory budget.
          # Snapshots are function-table-paired: the app must bake and serve a
          # separate init snapshot for this binary.
          # Ships as bin-slim/lean.{js,wasm} — same filenames as the main
          # binary so the glue's internal wasm reference and the workers'
          # hardcoded lean.js stay valid; deployments select the variant by
          # path prefix, not by filename.
          # emscripten-exports-slim.txt is NOT "main minus all ___boxed": the
          # extern/@[export] boxed stems must stay (body-less externs have no
          # IR to fall back on) — only the wholesale boxed-wrapper set goes.
          export NODE_OPTIONS="--max-old-space-size=8192"
          cp "build/$TARGET_STAGE/emscripten-exports-slim.txt" linkkit/emscripten-exports.txt
          bash docker-wasm/relink-local.sh linkkit /tmp/slim-out -O3
          dir=$(echo lean-*-*)
          mkdir -p "$dir/bin-slim"
          cp /tmp/slim-out/lean.js /tmp/slim-out/lean.wasm "$dir/bin-slim/"
          ls -la "$dir"/bin-slim/
      - name: Test browser API with the native i386 toolchain
        if: matrix.browser && !matrix.wasm
        run: |
          python3 tests/wasm/cli-smoke.py "build/$TARGET_STAGE/bin/lean"
          build/$TARGET_STAGE/bin/lean --run tests/wasm/native-smoke.lean
      - name: Test persistent WASM API in Node
        if: matrix.wasm
        timeout-minutes: 10
        run: |
          node tests/wasm/node-smoke.cjs "build/$TARGET_STAGE"
          node tests/wasm/node-smoke.cjs "build/$TARGET_STAGE" /tmp/slim-out --init-only
      - name: Test persistent WASM API in Chromium
        if: matrix.wasm
        timeout-minutes: 15
        run: |
          npm ci --prefix tests/wasm --ignore-scripts --no-audit --no-fund
          tests/wasm/node_modules/.bin/playwright install --with-deps chromium
          node tests/wasm/browser-smoke.mjs "build/$TARGET_STAGE"
          node tests/wasm/browser-smoke.mjs "build/$TARGET_STAGE" /tmp/slim-out --init-only
      - name: Test WASM snapshot round trips
        if: matrix.wasm
        timeout-minutes: 10
        run: |
          node --stack-size=8192 tests/wasm/bake-snapshot.cjs "build/$TARGET_STAGE" /tmp/full-snapshot
          node tests/wasm/browser-smoke.mjs "build/$TARGET_STAGE" --init-only --snapshot=/tmp/full-snapshot/init.snap
          node --stack-size=8192 tests/wasm/bake-snapshot.cjs "build/$TARGET_STAGE" /tmp/slim-snapshot /tmp/slim-out
          node tests/wasm/browser-smoke.mjs "build/$TARGET_STAGE" /tmp/slim-out --init-only --snapshot=/tmp/slim-snapshot/init.snap
      - name: Check Binaries
        if: matrix.binary-check
        run: ${{ matrix.binary-check }} lean-*/bin/* || true
      - name: Count binary symbols
        run: |
          for f in lean-*/bin/*; do
            echo "$f: $(nm $f | grep " T " | wc -l) exported symbols"
          done
        if: matrix.name == 'Windows'
      - name: List Install Tree
        run: |
          # omit contents of Init/, ...
          tree --du -h lean-*-* | grep -E ' (Init|Lean|Lake|LICENSE|[a-z])'
      - name: Pack
        run: |
          dir=$(echo lean-*-*)
          mkdir pack
          # high-compression tar.zst + zip for release, fast tar.zst otherwise
          if [[ '${{ startsWith(github.ref, 'refs/tags/') && matrix.release }}' == true || -n '${{ inputs.nightly }}' || -n '${{ inputs.RELEASE_TAG }}' ]]; then
            ${{ matrix.tar || 'tar' }} cf - $dir | zstd -T0 --no-progress -19 -o pack/$dir.tar.zst
            zip -rq pack/$dir.zip $dir
          else
            ${{ matrix.tar || 'tar' }} cf - $dir | zstd -T0 --no-progress -o pack/$dir.tar.zst
          fi
      - uses: actions/upload-artifact@v7
        if: matrix.release
        with:
          name: build-${{ matrix.name }}
          path: pack/*
          compression-level: 0
      - name: Lean stats
        run: |
          build/$TARGET_STAGE/bin/lean --stats src/Lean.lean
        if: ${{ !matrix.cross }}
      - name: Test
        id: test
        run: |
          ulimit -c unlimited  # coredumps
          time ctest --preset ${{ matrix.CMAKE_PRESET || 'release' }} --test-dir build/$TARGET_STAGE -j$NPROC --output-junit test-results.xml ${{ matrix.CTEST_OPTIONS }}
        if: matrix.test
      # copy failed tests' output into the `<failure>` elements shown by the test summary
      - name: Embed Test Output
        run: |
          python3 script/junit_embed_output.py build/$TARGET_STAGE/test-results.xml
        if: always() && steps.test.conclusion == 'failure'
      - name: Test Summary
        uses: test-summary/action@37b508cfee6d4d080eedd00b5bb240a6a784a6a5 # v2.6
        with:
          paths: build/${{ env.TARGET_STAGE }}/test-results.xml
        # prefix `if` above with `always` so it's run even if tests failed
        if: always() && steps.test.conclusion != 'skipped'
      - name: Check Test Binary
        run: ${{ matrix.binary-check }} tests/compile/534.lean.out
        if: matrix.binary-check && (!matrix.cross) && steps.test.conclusion != 'skipped'
      - name: Build Stage 2
        run: |
          make -C build -j$NPROC stage2
        if: matrix.test-bench
      - name: Check Stage 3
        run: |
          make -C build -j$NPROC check-stage3
        if: matrix.check-stage3
      - name: Test Benchmarks
        run: |
          cd tests
          nix develop -c make -C ../build -j$NPROC bench
        if: matrix.test-bench
      - name: Check rebootstrap
        run: |
          set -e
          git config user.email "stage0@lean-fro.org"
          git config user.name "update-stage0"
          make -C build update-stage0
          git commit --allow-empty -m "chore: update-stage0"
          time make -C build -j$NPROC
          time ctest --preset ${{ matrix.CMAKE_PRESET || 'release' }} --test-dir build/stage1 -j$NPROC
        if: matrix.check-rebootstrap
      - name: CCache stats
        if: always()
        run: ccache -s
      - name: Show stacktrace for coredumps
        if: failure() && runner.os == 'Linux'
        run: |
          for c in $(find . -name core); do
            progbin="$(file $c | sed "s/.*execfn: '\([^']*\)'.*/\1/")"
            echo bt | $GDB/bin/gdb -q $progbin $c || true
          done
